'use client'

import { FormEvent, useEffect, useState } from 'react'
import AppShell from '@/components/AppShell'
import RequireAuth from '@/components/RequireAuth'
import { api } from '@/lib/api'

export default function SettingsPage() { const [name, setName] = useState(''); const [headline, setHeadline] = useState(''); const [roles, setRoles] = useState(''); const [locations, setLocations] = useState(''); const [skills, setSkills] = useState(''); const [remote, setRemote] = useState(false); const [message, setMessage] = useState(''); const [error, setError] = useState(''); useEffect(() => { api.profile.get().then(result => { const profile = result.profile || {}; setName(String(profile.full_name || '')); setHeadline(String(profile.headline || '')); setRoles(Array.isArray(profile.target_roles) ? profile.target_roles.join(', ') : ''); setLocations(Array.isArray(profile.target_locations) ? profile.target_locations.join(', ') : ''); setSkills(Array.isArray(profile.skills) ? profile.skills.map((item: unknown) => typeof item === 'string' ? item : (item as { name?: string }).name || '').join(', ') : ''); setRemote(Boolean(profile.is_remote_preferred)) }).catch(err => setError(err instanceof Error ? err.message : 'Could not load profile.')) }, []); async function save(e: FormEvent) { e.preventDefault(); setMessage(''); setError(''); try { await api.profile.save({ full_name: name, headline, target_roles: roles.split(',').map(v => v.trim()).filter(Boolean), target_locations: locations.split(',').map(v => v.trim()).filter(Boolean), skills: skills.split(',').map(v => v.trim()).filter(Boolean), is_remote_preferred: remote }); setMessage('Profile saved. TalentRadar will use it for future recommendations.') } catch (err) { setError(err instanceof Error ? err.message : 'Could not save profile.') } } return <RequireAuth><AppShell><section className="page-heading"><p className="eyebrow">PROFILE & SETTINGS</p><h1>Set the direction of your search.</h1><p>Your profile informs role relevance, skill signals, and AI coaching.</p></section><form className="card-form settings-form" onSubmit={save}><label>Full name<input value={name} onChange={e => setName(e.target.value)} /></label><label>Professional headline<input value={headline} onChange={e => setHeadline(e.target.value)} placeholder="e.g. Product designer focused on fintech" /></label><label>Target roles <small>Separate with commas</small><input value={roles} onChange={e => setRoles(e.target.value)} placeholder="Product Designer, UX Researcher" /></label><label>Preferred locations <small>Separate with commas</small><input value={locations} onChange={e => setLocations(e.target.value)} placeholder="Bengaluru, Remote" /></label><label>Skills <small>Separate with commas</small><input value={skills} onChange={e => setSkills(e.target.value)} placeholder="Figma, User research, Systems thinking" /></label><label className="toggle-row"><input type="checkbox" checked={remote} onChange={e => setRemote(e.target.checked)} /> Prefer remote opportunities</label>{error && <p className="form-error">{error}</p>}{message && <p className="success-message">{message}</p>}<button className="primary-button">Save profile</button></form></AppShell></RequireAuth> }
